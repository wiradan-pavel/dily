import { SellUs } from "../../Sections";

import style from "./style.module.scss";

const SellUsWrapper = () => {
  return (
    <div className={`container ${style.wrapper}`}>
      <SellUs />
    </div>
  );
};

export default SellUsWrapper;
